# Usen esto para instalar las dependencias de ahora en adelante. Entiéndase dependencias como los módulos que van a usar.
```
# pip install -r requirements.txt (para Windows) ó py -m pip install -r requirements.txt (por si da problemas en el cmd/powershell)  
# / 
# python3 pip install -r requirements.txt (para Linux y macOS)
```
django-bootstrap-v5